import { createTheme } from "@mui/material";

const customeTheme = createTheme({
    palette: {
        mode: "light",
        primary: {
            main: "#00927c"
        }, 
        secondary: {
            main: "#eaf0f1"
        },
    }
})

export default customeTheme;